---
apiVersion: v1
kind: Service
metadata:
  name: airflow-api-server-nodeport
  namespace: airflow
spec:
  type: NodePort
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 30080
    protocol: TCP
    name: airflow-ui
  selector:
    component: api-server
    release: airflow
